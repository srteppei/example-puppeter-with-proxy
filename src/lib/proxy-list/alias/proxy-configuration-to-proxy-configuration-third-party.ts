import { Options } from 'proxy-lists'

import { ProxyConfiguration, MapInterface } from '../..'

export type ProxyConfigurationToProxyConfigurationThirdParty = MapInterface<ProxyConfiguration, Options>
